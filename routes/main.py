routes = {
    #the keys within our nested dictionary will handle pointing to our specific file.

    "/" : {
        "template" : "a file"
    },
    "/goodbye" : {
        "template" : "a file"
    }
}
